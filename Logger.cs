using System;

namespace DemoApp
{
    internal class Logger
    {
        public static void Debug(string message)
        {
            // Uses som log library
            Console.WriteLine(message);
        }
    }
}